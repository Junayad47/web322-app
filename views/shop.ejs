<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop</title>
    <link rel="stylesheet" href="/public/css/main.css">
    <!-- Adding Bootstrap CSS for better styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KyZXEAg3QhqLMpG8r+8fhAXLRsO2vvoC2f3Bz+83SK7+t3ZK/2T9ht4j4K2zT6j5" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <a class="navbar-brand" href="#">Junayad Bin Forhad - Assignment 6</a>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item"><a class="nav-link <%= activeRoute === '/shop' ? 'active' : '' %>"
                            href="/shop">Shop</a></li>
                    <li class="nav-item"><a class="nav-link <%= activeRoute === '/about' ? 'active' : '' %>"
                            href="/about">About</a></li>
                    <% if (session.user) { %>
                        <li class="nav-item"><a class="nav-link <%= activeRoute === '/items' ? 'active' : '' %>"
                                href="/items">Items</a></li>
                        <li class="nav-item"><a class="nav-link <%= activeRoute === '/items/add' ? 'active' : '' %>"
                                href="/items/add">Add Item</a></li>
                        <li class="nav-item"><a class="nav-link <%= activeRoute === '/categories' ? 'active' : '' %>"
                                href="/categories">Categories</a></li>
                        <li class="nav-item"><a class="nav-link <%= activeRoute === '/userHistory' ? 'active' : '' %>"
                                href="/userHistory">User History</a></li>
                        <li class="nav-item"><a class="nav-link" href="/logout">Log Out</a></li>
                        <% } else { %>
                            <li class="nav-item"><a class="nav-link <%= activeRoute === '/register' ? 'active' : '' %>"
                                    href="/register">Register</a></li>
                            <li class="nav-item"><a class="nav-link <%= activeRoute === '/login' ? 'active' : '' %>"
                                    href="/login">Log In</a></li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container" style="padding-top: 90px;">
        <div class="row">
            <div class="col-md-8">
                <% if (data.item) { %>
                    <article>
                        <h2>
                            <%= data.item.title %>
                        </h2>
                        <img src="<%= data.item.featureImage %>" alt="<%= data.item.title %>" class="img-fluid">
                        <br><br>
                        <%= helpers.safeHTML(data.item.body) %>
                            <br><br>
                            <strong>Category:</strong>
                            <%= data.item.category %><br>
                                <strong>Price:</strong> $<%= data.item.price %><br>
                                    <strong>Last Updated:</strong>
                                    <%= data.item.itemDate %>
                    </article>
                    <% } else { %>
                        <div class="col-md-12 text-center">
                            <h2>
                                <%= data.message %>
                            </h2>
                            <p>Please try another Item / Category</p>
                        </div>
                        <% } %>
            </div>
            <div class="col-md-4">
                <% if (data.items && data.items.length> 0) { %>
                    <h4>Items</h4>
                    <ul class="list-group">
                        <% data.items.forEach(item=> { %>
                            <li class="list-group-item"><a href="/shop/<%= item.id %>?category=<%= viewingCategory %>">
                                    <%= item.title %>
                                </a></li>
                            <% }) %>
                    </ul>
                    <% } %>
                        <% if (data.categories && data.categories.length> 0) { %>
                            <h4>Categories</h4>
                            <ul class="list-group">
                                <% data.categories.forEach(category=> { %>
                                    <li class="list-group-item"><a href="/shop?category=<%= category.id %>">
                                            <%= category.name %>
                                        </a></li>
                                    <% }) %>
                            </ul>
                            <% } %>
            </div>
        </div>
    </div>
    <!-- Adding Bootstrap JS for better functionality -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe"
        crossorigin="anonymous"></script>
</body>

</html>